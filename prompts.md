```
Provide me a zip with 10 simple processing examples. each examples should be placed in a dedicated directory. The name of the directory should start with a number. e.g. 03_circle_animation. The prefix number of the directories should start with "61_". Remember to always have a draw() { ... } function even if the example actually doesn't require the animation loop. Just keep the draw() {} function empty if no animation is expected.

```
