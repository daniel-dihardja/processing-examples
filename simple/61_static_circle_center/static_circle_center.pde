// This example draws a static circle in the center of the canvas.

void setup() {
  size(400, 400);
  background(255);
  fill(100, 150, 255);
  ellipse(width / 2, height / 2, 100, 100);
}

void draw() {
  // No animation needed
}
